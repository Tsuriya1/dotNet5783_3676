<Window x:Class="PL.Order.UpdateAndActionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Order"
        mc:Ignorable="d"
        Title="UpdateAndActionsWindow" Height="450" Width="800">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions >
        <Label Content="{Binding Order}" HorizontalContentAlignment="Center" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" />
        <Button Content="Update Shipping" Click="updateShippingClick" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"  Grid.Row="1" />
        <Button Content="Update Supply" Click="updateSupplyClick"  VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Grid.Row="2"/>

    </Grid>
</Window>

